# Kubernetes - Amazon EKS

- What is EKS? Setup AWS CLI and IAM Role for EKS
EKS (Elastic Kubernetes Service)
AWS CLI
IAM Role for EKS

    1. Create an AWS account

    1. Extract access keys

    1. Install AWS CLI

    1. Log in via cli to aws using access keys

- Create a VPC for your cluster
    1. CloudFormation create a stack following roles created for EKS Cluster.
    1. To create 'EKS-Cluser-Trail1'.Subnets created via the CloudFormation from amazon-eks-vpc-private-subnets.yaml
- Spin up our cluster with EKS

- Launch worker nodes into our cluster
